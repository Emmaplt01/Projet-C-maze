# Projet-C-maze
Le projet maze consistait à écrire un programme capable de résoudre automatiquement un labyrinthe en tenant toujours la gauche.

Pour exécuter ce projet :
gcc -Wall -Wextra -Werror -Iinclude src/*.c -o maze
./maze map.txt
